{
  "name": "burak",
  "version": "1.0.0",
  "description": "This is burak project Back end",
  "main": "server.ts",
  "scripts": {
    "start": "ts-node src/server.ts",
    "start:dev": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/server.ts",
    "build": "tsc",
    "dev": "npm run start:dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ADAM",
  "license": "ISC",
  "dependencies": {
    "moment": "^2.30.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "nodemon": "^3.1.7",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.2.2"
  }
}
